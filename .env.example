# Optional — accounts. Without these the app runs anonymously and hides sign-in.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# After enabling OAuth providers in Supabase, list them to show the buttons:
# NEXT_PUBLIC_AUTH_PROVIDERS=google,apple
